# This directory contains trained model artifacts generated by the ML pipeline.  
# Running the training scripts will produce the following files:  
# - recipe_vectors.npy — vectorized recipe representations  
# - tfidf_vectorizer.pkl — the fitted TF-IDF vectorizer 